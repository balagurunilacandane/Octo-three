/** Imperative camera handle used by DOM UI (reset / zoom / rotate buttons). Filled in by <WorldCamera>. */
export const cameraApi = {
  reset: () => {},
  zoomBy: (_k: number) => {},
  rotate: (_rad: number) => {},
}
